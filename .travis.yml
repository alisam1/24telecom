language: node_js

node_js:
  - "10.16.0"

notifications:
  email: false

script: gulp build

deploy:
edge:
    branch: v1.8.47
  provider: pages
  skip-cleanup: true
  local-dir: build
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master